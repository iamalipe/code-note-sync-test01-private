Sync Between SafalSubscripton and SafalMyBuy

Scenario 01

1. Create a Event ‘EVT01’ in SafalSub
2. It also create a copy in SafalMyBuy with same info
3. i can edit in SafalSub => It Sync with SafalMyBuy (Yes|No) - Yes
4. i can delete in SafalSub => It Sync with SafalMyBuy (Yes|No) - Yes  
    - If already linked to SafalMyBuy purchase item, then what happend ?  
    - ANS: ask popup for conformation
5. i can edit in SafalMyBuy => It Sync with SafalSub (Yes|No) - Yes (Only edit Budget-Popup ask)
6. i can delete in SafalMyBuy => It Sync with SafalSub (Yes|No) - No  
    - If people are invited to this event, then what happend ? - No
7. if disconnect then what happend to ‘EVT01’ in SafalSub?  
    - All linked event are inactive in SafalMyBuy. (and show warnning that n number event going to inactive)
8. if disconnect then what happend to ‘EVT01’ in SafalMyBuy?  
    - All linked event are inactive in SafalMyBuy. (and show warnning that n number event going to inactive)

Scenario 02

1. Create a Event ‘EVT02’ in SafalMyBuy
2. It also create a copy in SafalSub with same info
3. i can edit in SafalMyBuy => It Sync with SafalSub (Yes|No) - Yes (Full Edit)  
    - SafalSub also can edit full info
4. i can delete in SafalMyBuy => It Sync with SafalSub (Yes|No) - If no Activity in SafalSub then - Yes

if disconnect happend in SafalSub then what happend all event which created in SafalSub ?  
- Event which in SafalSub that do nothing  
if disconnect happend in SafalSub then what happend all event which created in SafalMyBuy ?  
- Event which in SafalSub that do nothing  
if disconnect happend in SafalMyBuy then what happend all event which created in SafalMyBuy ?  
- Event which in SafalSub that do nothing  
if disconnect happend in SafalMyBuy then what happend all event which created in SafalSub ?  
- Event which in SafalSub that do nothing